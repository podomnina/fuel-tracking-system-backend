package com.startup.controller;

public final class ApiPath {
    public static final String SHIP = "ship",
    ID = "{id}",
    ROUTE = "route",
    PORT = "port";


    public static final String SHIP_PATH = "/" + SHIP,
    ID_PATH = "/" + ID,
    ROUTE_PATH = "/" + ROUTE,
    PORT_PATH = "/" + PORT;
}
